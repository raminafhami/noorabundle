export enum RedisPrefixEnum {
  VERIFICATION_CODE = 'verification_code',
  REFRESH_TOKEN = 'refresh_token',
  CONTRACT_VERIFICATION_CODE = 'contract_verification_code',
  DISPATCHER = 'dispatcher',
}
