export interface NotificationMessages {
  title: string;
  description: string;
  category: string;
  priority: string;
  readedAt: string | null;
  userId: string;
  notificationId: string;
  createdAt: string;
  id: string;
}
